# Artifact Atlas fixture

Markdown/code smoke-test content for the flags-on AssetViewer format sweep (P6-009).

- Renders via `ContentRenderer` (`@miethe/ui/content-viewer`).
- Exercises the `text/markdown` renderer path.
